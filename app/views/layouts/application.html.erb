<!DOCTYPE html>
<html itemscope itemtype="http://schema.org/WebPage" lang="fr">
  <head>
    <!-- Partial that takes care of all the meta -->
    <%= render partial: 'layouts/meta_common' %>

    <!-- Load the assets -->
    <link href='https://fonts.googleapis.com/css?family=Open+Sans' rel='stylesheet' type='text/css'>
    <%= stylesheet_link_tag 'application', media: 'all' %>

    <%= csrf_meta_tags %>
    <%= render partial: "layouts/analytics_meta" %>

    <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
    <!--[if lt IE 9]> <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script> <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script> <![endif]-->

  </head>
  <body>

    <%= render partial: 'layouts/public_elements/navbar' %>

    <!-- Notice container for alert send by controller -->
    <!-- For the style can be handled in the page itself not in the layout -->
    <% if !content_for?(:notice_container) %>
    <div class="container">
      <% flash.each do |key, value| %>
      <div class="text-center alert-warning alert alert-<%= key %> " id="flash-message">
        <strong><%= value %></strong>
      </div>
      <% end %>
    </div>
    <% end %>

    <!-- Yield content of the page -->
    <%= yield %>

    <!-- Footer partial -->
    <%= render partial: 'layouts/public_elements/footer'%>


    <!-- Load Javascript for public pages -->
    <%= javascript_include_tag 'application'%>

    <!-- Endscript container -->
    <%= render partial: 'layouts/end_scripts' %>

  </body>
</html>
